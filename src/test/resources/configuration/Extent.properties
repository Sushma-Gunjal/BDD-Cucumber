# Specify the report title
report.title = My Extent Report

# Specify the report name
report.name = Test Report

# Specify the theme (standard, dark, or light)
report.theme = standard

# Specify the document title
report.document.title = Extent Report

# Specify the report directory path
report.dir = test-output/

# Specify the report file name
report.file.name = index.html

# Configure the report's system info (key-value pairs)
system.info.os = Windows
system.info.browser = Chrome
system.info.env = QA

# Configure the report's document title
report.document.title = My Extent Report

# Configure the report's report name
report.name = Test Report

screenshot.dir=Screenshots/

# Configure the report's encoding
report.encoding = UTF-8

